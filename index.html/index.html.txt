<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Exclusive Free Offer – Limited Spots</title>
  <meta name="description" content="Clean, fast CPA landing page template. Mobile‑first, SEO‑friendly, with tracking placeholders and UTM auto‑forwarding." />
  <meta property="og:title" content="Exclusive Free Offer" />
  <meta property="og:description" content="Grab this limited-time deal in 3 simple steps." />
  <meta property="og:type" content="website" />
  <meta property="og:image" content="https://dummyimage.com/1200x630/efefef/333&text=Your+Offer+Image" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b0f1a; --card:#12182a; --muted:#8a94a7; --text:#ecf0ff; --accent:#7c5cff; --accent-2:#18c964;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,'Helvetica Neue',Arial,sans-serif;
      background:linear-gradient(180deg,#0b0f1a, #0e1526 40%, #0b0f1a 100%);
      color:var(--text);
    }
    .container{max-width:1100px;margin:0 auto;padding:24px}
    .nav{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 0}
    .brand{display:flex;align-items:center;gap:10px;font-weight:700}
    .brand .logo{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#5ad0ff)}
    .btn{display:inline-flex;align-items:center;justify-content:center;padding:12px 18px;border-radius:12px;text-decoration:none;font-weight:600;transition:transform .06s ease,opacity .2s ease;cursor:pointer;border:0}
    .btn:hover{transform:translateY(-1px)}
    .btn:active{transform:translateY(0)}
    .btn.primary{background:var(--accent);color:white}
    .btn.secondary{background:#1a2237;color:var(--text);border:1px solid #2a3554}
    .hero{display:grid;grid-template-columns:1.2fr .8fr;gap:28px;align-items:center;padding:32px 0}
    .hero h1{font-size:clamp(28px,4vw,44px);line-height:1.1;margin:0 0 12px}
    .hero p{color:var(--muted);font-size:clamp(14px,2.1vw,17px)}
    .badge{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:999px;background:#131a2f;border:1px solid #263155;color:#c8d0ea;font-weight:600;font-size:13px}
    .card{background:var(--card);border:1px solid #1b2541;border-radius:18px;box-shadow:0 10px 30px rgba(0,0,0,.35)}
    .hero-card{padding:18px}
    .hero-img{width:100%;border-radius:14px;display:block;border:1px solid #1e2744}
    .grid{display:grid;gap:16px}
    .grid-3{grid-template-columns:repeat(3,1fr)}
    .grid-2{grid-template-columns:repeat(2,1fr)}
    @media (max-width:900px){.hero{grid-template-columns:1fr}.grid-3{grid-template-columns:1fr}.grid-2{grid-template-columns:1fr}}
    .step{padding:16px}
    .step h3{margin:0 0 6px;font-size:18px}
    .step p{margin:0;color:#b8c0da;font-size:14px}
    .trust{display:flex;gap:16px;flex-wrap:wrap;align-items:center}
    .trust img{height:22px;opacity:.8}
    .cta-wrap{display:flex;gap:12px;flex-wrap:wrap;margin-top:16px}
    .section{padding:30px 0}
    .check{display:flex;gap:10px;align-items:flex-start;color:#cde6d8}
    .check svg{flex:0 0 auto}
    .footer{color:#98a2c3;font-size:13px;padding:22px 0}
    .fine{color:#8a94a7;font-size:13px}
    .notice{background:#0e1a2a;border-left:3px solid #1971f5;color:#cbd6ff;padding:12px 14px;border-radius:10px}
    .pill{padding:6px 10px;border:1px solid #2a3554;border-radius:999px;color:#bcd}
    .faq-item{padding:14px}
    details{background:#0e1426;border:1px solid #1a2442;border-radius:12px;padding:10px}
    summary{cursor:pointer;font-weight:600}
    .label{font-size:12px;color:#9aa7c7;margin:8px 0 2px}
    input,select{width:100%;padding:12px 14px;border-radius:12px;border:1px solid #263358;background:#10182c;color:#e5ecff}
    .form{display:grid;gap:12px}
  </style>

  <!-- Tracking placeholders (paste your scripts and IDs) -->
  <!-- Google Tag / FB Pixel / Voluum etc. -->
  <!-- <script>/* your analytics */</script> -->
</head>
<body>
  <header class="container nav">
    <div class="brand">
      <div class="logo" aria-hidden="true"></div>
      <span>OfferHub</span>
    </div>
    <div class="cta-wrap">
      <a class="btn secondary" href="#faq">FAQ</a>
      <a class="btn primary" id="ctaTop" href="#">Get Offer</a>
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <div>
        <span class="badge">No purchase required • Mobile friendly • ~2 min</span>
        <h1>Claim your <span style="color:var(--accent-2)">free reward</span> in 3 simple steps</h1>
        <p>Follow the instructions, complete a quick verification, and receive your reward instantly. Available for a limited time in your region.</p>
        <div class="cta-wrap">
          <a class="btn primary" id="ctaHero" href="#">Start Now</a>
          <a class="btn secondary" href="#how">How it works</a>
        </div>
        <div style="height:14px"></div>
        <div class="trust">
          <span class="pill">SSL Secured</span>
          <span class="pill">No hidden fees</span>
          <span class="pill">Works on iOS & Android</span>
        </div>
      </div>
      <div class="card hero-card">
        <img class="hero-img" src="https://dummyimage.com/720x480/0e1526/ffffff&text=Your+Offer+Image+or+Mockup" alt="Offer mockup" />
        <div style="padding:12px 6px 4px">
          <div class="check"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20 7L9 18l-5-5" stroke="#18c964" stroke-width="2" stroke-linecap="round"/></svg> No credit card required for basic entry.</div>
          <div class="check"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M20 7L9 18l-5-5" stroke="#18c964" stroke-width="2" stroke-linecap="round"/></svg> New users only. Limited redemptions daily.</div>
        </div>
      </div>
    </section>

    <section id="how" class="section">
      <h2 style="margin:0 0 12px">How it works</h2>
      <div class="grid grid-3">
        <div class="card step">
          <h3>1) Tap Start</h3>
          <p>You’ll be forwarded to a secure, partner page.</p>
        </div>
        <div class="card step">
          <h3>2) Complete Requirements</h3>
          <p>Finish a quick app install or email signup to qualify.</p>
        </div>
        <div class="card step">
          <h3>3) Receive Reward</h3>
          <p>Get confirmation on-screen. Rewards are limited each day.</p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="grid grid-2">
        <div class="card" style="padding:18px">
          <h3 style="margin-top:0">Fast Tips for Higher Approval</h3>
          <ul>
            <li>Use your real email/phone number.</li>
            <li>Turn off any ad blockers during verification.</li>
            <li>Complete all steps on the partner page.</li>
          </ul>
          <div class="notice">Note: This site may earn a commission if you complete an eligible action.</div>
        </div>
        <div class="card" style="padding:18px">
          <h3 style="margin-top:0">Optional: Join the List</h3>
          <p class="fine">Get alerts when new rewards go live in your country.</p>
          <form class="form" id="leadForm">
            <div>
              <div class="label">Email</div>
              <input type="email" id="email" placeholder="you@example.com" required />
            </div>
            <div>
              <div class="label">Country</div>
              <select id="country">
                <option value="">Select…</option>
                <option>Kenya</option>
                <option>United States</option>
                <option>United Kingdom</option>
                <option>Nigeria</option>
                <option>India</option>
                <option>Philippines</option>
              </select>
            </div>
            <button class="btn primary" type="submit">Get Alerts</button>
            <div id="leadMsg" class="fine" aria-live="polite"></div>
          </form>
        </div>
      </div>
    </section>

    <section id="faq" class="section">
      <h2 style="margin:0 0 12px">FAQ</h2>
      <div class="grid">
        <div class="faq-item">
          <details>
            <summary>Is this offer free?</summary>
            <p>Basic entry is free. Some partner pages may present optional paid trials — these are not required unless explicitly stated by the offer terms.</p>
          </details>
        </div>
        <div class="faq-item">
          <details>
            <summary>How do I get my reward?</summary>
            <p>After completing the required action, you’ll see an on‑screen confirmation from the partner. Keep any emails/SMS for your records.</p>
          </details>
        </div>
        <div class="faq-item">
          <details>
            <summary>Is my information safe?</summary>
            <p>We don’t sell your data. See our Privacy Policy below. Actions you take on partner sites are governed by their policies.</p>
          </details>
        </div>
      </div>
    </section>

    <section class="section" style="text-align:center">
      <a class="btn primary" id="ctaBottom" href="#">Claim Offer</a>
      <p class="fine" style="margin-top:10px">By continuing you agree to the <a href="#" style="color:#9fd3ff">Terms</a> and acknowledge our <a href="#" style="color:#9fd3ff">Privacy</a>.</p>
    </section>

    <footer class="container footer">
      <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap">
        <div>© <span id="year"></span> OfferHub. All rights reserved.</div>
        <div style="display:flex;gap:12px"><a href="#" style="color:#c8d0ea">Privacy</a><a href="#" style="color:#c8d0ea">Terms</a><a href="#" style="color:#c8d0ea">Contact</a></div>
      </div>
      <div class="fine" style="margin-top:8px">This site is an independent promotion and is not affiliated with or endorsed by any brand mentioned. Availability varies by location.</div>
    </footer>
  </main>

  <script>
    // ========== CONFIG ==========
    // 1) Put your CPA offer link here (fallback)
    const DEFAULT_OFFER_URL = 'https://example.com/your-cpa-offer';

    // 2) (Optional) Allow override via URL, e.g. ?offer=https://yourlink
    // 3) Auto-append UTM + all query params from this page to the outbound link

    const qp = new URLSearchParams(window.location.search);
    const offerFromParam = qp.get('offer');
    const baseOffer = (offerFromParam && offerFromParam.startsWith('http')) ? offerFromParam : DEFAULT_OFFER_URL;

    function buildOutboundUrl() {
      try {
        const url = new URL(baseOffer);
        // Forward all query params from current page to the offer (preserve tracking)
        qp.forEach((v, k) => {
          if(k !== 'offer') url.searchParams.set(k, v);
        });
        // Basic UTM defaults, don’t overwrite if already present
        if(!url.searchParams.get('utm_source')) url.searchParams.set('utm_source','lp');
        if(!url.searchParams.get('utm_medium')) url.searchParams.set('utm_medium','cta');
        if(!url.searchParams.get('utm_campaign')) url.searchParams.set('utm_campaign','main');
        return url.toString();
      } catch(e){
        return baseOffer; // fallback
      }
    }

    function wireCTAs(){
      const href = buildOutboundUrl();
      ['ctaTop','ctaHero','ctaBottom'].forEach(id=>{
        const el = document.getElementById(id);
        if(el) el.setAttribute('href', href);
      });
    }

    // lightweight fake opt‑in handler (replace with your ESP/Form backend)
    const leadForm = document.getElementById('leadForm');
    if(leadForm){
      leadForm.addEventListener('submit', function(e){
        e.preventDefault();
        const email = document.getElementById('email').value.trim();
        const country = document.getElementById('country').value;
        const msg = document.getElementById('leadMsg');
        // TODO: replace with your API request (Formspree, GetResponse, Beehiiv, Mailchimp, etc.)
        // Example stub success:
        setTimeout(()=>{
          msg.textContent = 'Thanks! You\'re on the list. Keep an eye on your inbox.';
        }, 400);
      });
    }

    document.getElementById('year').textContent = new Date().getFullYear();
    wireCTAs();
  </script>
</body>
</html>
